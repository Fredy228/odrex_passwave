export enum EPrivilegeType {
  GROUP = "group",
  USER = "user",
}

export enum EPrivilegeList {
  COMPANY = "company",
  HALL = "hall",
  DEVICE = "device",
  PASSWORD = "password",
}

export enum EPrivilegeDirection {
  UP = "up",
  DOWN = "down",
}

export enum Permit {
  READ = "read",
  EDIT = "edit",
}

export enum PermitEdit {
  READ = "read",
  READ_ALL = "read-all",
  EDIT = "edit",
  EDIT_ALL = "edit-all",
  NOT = "not",
}
